global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ais-tracking-system
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:3000
          - ingestion:3000
          - worker:3000
